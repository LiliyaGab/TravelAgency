@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<div class="panel">
    <div class="panel-heading">
        <h2>Личный кабинет</h2>
    </div>
    <div class="panel-body">
        <div>
            @using (@Html.BeginForm("Index", "Hotel"))
            {
                <input type="submit" class="btn btn-default btn-xs" value="Отели" />
            }
        </div>
        <div>
            @using (@Html.BeginForm("Index", "Tour"))
            {
                <input type="submit" class="btn btn-default btn-xs" value="Туры" />
            }
        </div>
    </div>

</div>
